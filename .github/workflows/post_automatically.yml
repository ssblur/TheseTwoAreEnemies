on:
  schedule:
    - cron:  '0 20 * * *'

jobs:
  post:
    runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v3
      with:
        python-version: '>=3.10'
        architecture: 'x64'
    - run: python . --twitter --once